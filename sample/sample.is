let f = @(x, y) {
    eval {
        if (x) < (100) {
            <: (x)
        } else {
            <: ("too big!")
        }
        [y (x) + (y)]
    }
}
let x = 0
var y = 1
for (let i, 20) {
    let xy = (f)(x, y)
    x = (xy)[0]
    y = (xy)[1]
}