var x = 0
var y = 1
for (let i, 20) {
    if (x) < (100) {
        <: (x)
    } else {
        <: ("too big!")
    }
    let f = @(x, y) {
        (x) + (y)
    }
    let z = f(x, y)
    x = y
    y = z
}
let xy = [x y]