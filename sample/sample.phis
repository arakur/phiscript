var x: int = 0; var y: int = 1
for i in 20
    if x < 100
        <: x
    else
        <: "too big!"
    end
    z: int -> int -> int := (x -> y -> Core::add(x, y)) x y
    x <- y; y <- z
end
let xy: [some, some] = [x, y]
let dict: {
    foo: int; bar: {baz: string}} = {foo: if x == 0 then 42 else 334 end;bar: {baz: "ok"}}
<: dict.bar.baz
