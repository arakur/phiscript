var x = 0; var y = 1
for i in 20
    if x < 100
        <: x
    else
        <: "too big!"
    end
    z := ((x, y) -> Core::add(x, y))(x, y)
    x <- y; y <- z
end
let xy = [x, y]
let dict = {foo: if x == 0 then 42 else 334 end;bar: {baz: "ok"}}
<: dict.bar.baz
