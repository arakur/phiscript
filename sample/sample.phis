var x: int = 0; var y: int = 1
for i in 20
    if x < 100
        <: x
    else
        <: "too big!"
    end
    f := x: int -> y: int -> Core::add(x, y)
    z: int := f x y
    x <- y; y <- z
end

let xy: [some, number] = [x, y]
xy[0]; xy.1

let obj: {
    foo: int; bar: {baz: string}} = {foo: if x == 0 then 42 else 334 end;bar: {baz: "ok"}}
<: obj.bar.baz
