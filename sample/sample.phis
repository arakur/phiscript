var x = 0
var y = 1

for i in 20
    if x < 100
        <: x
    else
        <: "too big!"
    end
    let f = (x, y) -> begin
        x + y
    end
    z := f(x, y)
    x <- y
    y = z
end

let xy = [x, y]
