let f = (x: int, y: int) -> begin
    if x < 100
        <: x
    else
        <: "too big!"
    end
    [y, x + y]
end

let x: int = 0
var y: int = 1

for i in 20
    xy := f(x, y)
    x: int <- xy[0]; y <- xy[1]
end
